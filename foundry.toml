[profile.default]
src = 'src'
out = 'out'
libs = ['lib']
solc_version = '0.8.24'
optimizer = true
optimizer_runs = 200
via_ir = false

# 以下为推荐添加的
[fmt]
line_length = 120  # 格式化代码的行长度

[fuzz]
runs = 256  # 模糊测试的运行次数

# 测试相关
[invariant]
runs = 256
depth = 30
